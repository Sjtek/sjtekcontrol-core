group 'nl.sjtek.control'
version '3.0'

allprojects {
    repositories {
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}

task wrapper(type: Wrapper) {
        gradleVersion = '3.4.1'
}
